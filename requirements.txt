# ── Python 3.10 compatible requirements for cloud deployment ──────────────────
# Tested against Python 3.10.x (pinned via .python-version / render.yaml)

# Core scientific stack — Python 3.10 compatible builds
numpy==1.23.5
scipy==1.9.3
astropy==5.1.1
matplotlib==3.6.3
pandas==1.5.3
Pillow==9.5.0

# Machine Learning
scikit-learn==1.1.3
joblib==1.2.0
threadpoolctl==3.1.0

# TensorFlow — 2.10.x is the last version supporting Python 3.10 on CPU
# Uses tf.compat.v1 API for AstroNet CNN backwards compatibility
tensorflow==2.10.1
tensorflow-estimator==2.10.0
keras==2.10.0

# TF dependencies
absl-py==1.4.0
astunparse==1.6.3
flatbuffers==23.5.26
gast==0.4.0
google-pasta==0.2.0
grpcio==1.59.3
h5py==3.7.0
libclang==16.0.6
opt-einsum==3.3.0
protobuf==3.19.6
six==1.16.0
termcolor==2.3.0
wrapt==1.14.1
typing_extensions==4.7.1

# Web server
tornado==6.3.3

# Astronomy data access (fetches from NASA MAST)
lightkurve>=2.4.0

# PDF report generation
reportlab>=4.0.0

# Explainability layer (graceful fallback if unavailable)
shap>=0.42.0

# Utilities
packaging==23.2
python-dateutil==2.8.2
pytz==2023.3
Werkzeug==2.3.7
Markdown==3.5.1
MarkupSafe==2.1.3
requests>=2.28.0
